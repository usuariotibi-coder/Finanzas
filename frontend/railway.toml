[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx serve -s dist -l tcp://0.0.0.0:$PORT"

[build.env]
NPM_CONFIG_PRODUCTION = "false"

[build.commands]
install = "npm ci"
build = "npm run build"
